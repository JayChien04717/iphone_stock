# 🚀 Streamlit Cloud 部署與 API 配置指南

## 📋 前置準備

### 1️⃣ 獲取免費 API Keys

#### Finnhub API Key
1. 訪問: https://finnhub.io/register
2. 使用 Email 註冊（完全免費）
3. 登入後，在 Dashboard 找到您的 API Key
4. **複製並保存** API Key（稍後需要）

#### Alpha Vantage API Key
1. 訪問: https://www.alphavantage.co/support/#api-key
2. 填寫表單（完全免費）
3. API Key 會立即發送到您的 Email
4. **複製並保存** API Key（稍後需要）

---

## 🌐 Streamlit Cloud 部署步驟

### 步驟 1: 登入 Streamlit Cloud

1. 訪問: https://share.streamlit.io/
2. 使用 GitHub 帳號登入
3. 授權 Streamlit 訪問您的 GitHub repositories

### 步驟 2: 部署應用

1. 點擊 **"New app"**
2. 選擇您的 repository: `JayChien04717/iphone_stock`
3. 設置：
   - **Branch**: `main`
   - **Main file path**: `app.py`
   - **App URL**: 選擇一個自定義 URL（例如：`jay-stock-valuation`）

4. **先不要點擊 Deploy！** 我們需要先配置 Secrets

### 步驟 3: 配置 API Secrets ⭐ 重要

1. 在部署頁面，找到 **"Advanced settings"**
2. 點擊展開
3. 找到 **"Secrets"** 區域
4. 在 Secrets 文本框中，輸入以下內容：

```toml
FINNHUB_API_KEY = "paste_your_finnhub_key_here"
ALPHA_VANTAGE_API_KEY = "paste_your_alpha_vantage_key_here"
```

**替換為您的實際 API Keys！**

**示例:**
```toml
FINNHUB_API_KEY = "abc123def456ghi789jkl"
ALPHA_VANTAGE_API_KEY = "XYZ789ABC123DEF456"
```

5. 確認 Secrets 已正確輸入
6. 點擊 **"Deploy!"**

### 步驟 4: 等待部署完成

- 部署通常需要 2-5 分鐘
- 您會看到部署日誌
- 等待顯示 "Your app is live!" ✅

---

## ✅ 驗證 API 配置

部署完成後：

1. 打開您的應用 URL
2. 查看側邊欄的 **"🔌 API Status"**
3. 應該顯示：
   - ✅ **Finnhub API: Active** (綠色)
   - ✅ **Alpha Vantage API: Active** (綠色)

4. 測試功能：
   - 分析一支股票（例如：AAPL）
   - 點擊 **"📰 News & Sentiment"** 標籤
   - 應該能看到最新新聞和分析師建議

---

## 🔧 如何更新 Secrets（已部署後）

如果您需要更新 API Keys：

1. 進入 Streamlit Cloud Dashboard
2. 找到您的應用
3. 點擊 **"⋮"** (三個點) → **"Settings"**
4. 找到 **"Secrets"** 區域
5. 更新您的 API Keys
6. 點擊 **"Save"**
7. 應用會自動重新部署

---

## 📱 在 iPhone 上使用

### 方法 1: 瀏覽器直接訪問
1. 在 Safari 打開您的應用 URL
2. 點擊 **"分享"** 按鈕
3. 選擇 **"加入主畫面"**
4. 設置圖標名稱（例如：Stock Valuation）
5. 完成！現在可以像 App 一樣使用

### 方法 2: PWA 安裝（推薦）
1. 在 Safari 訪問應用
2. 系統可能會提示安裝為 PWA
3. 點擊 **"安裝"**
4. 應用會添加到主畫面

---

## ⚠️ 重要提示

### API 使用限制
- **Finnhub**: 60 calls/minute（足夠使用）
- **Alpha Vantage**: 25 calls/day（謹慎使用）

### 安全性
- ✅ Secrets 在 Streamlit Cloud 中是加密的
- ✅ 不會在代碼中顯示
- ✅ 不會被提交到 Git
- ❌ 不要在公開場合分享您的 API Keys

### 故障排除

**如果 API 狀態顯示 "Not configured":**
1. 檢查 Secrets 格式是否正確（TOML 格式）
2. 確認 API Keys 沒有多餘的空格或引號
3. 確認 Key 名稱完全一致：
   - `FINNHUB_API_KEY`
   - `ALPHA_VANTAGE_API_KEY`
4. 重新部署應用

**如果新聞功能不工作:**
1. 確認 Finnhub API Key 有效
2. 檢查是否超過免費額度
3. 查看應用日誌（Streamlit Cloud → Manage app → Logs）

---

## 🎯 完整配置檢查清單

- [ ] 已獲取 Finnhub API Key
- [ ] 已獲取 Alpha Vantage API Key
- [ ] 已在 Streamlit Cloud 創建應用
- [ ] 已在 Secrets 中配置兩個 API Keys
- [ ] 應用已成功部署
- [ ] 側邊欄顯示 API 狀態為 Active
- [ ] 新聞與情緒分析功能正常工作
- [ ] 已添加到 iPhone 主畫面

---

## 📞 需要幫助？

如果遇到問題：
1. 查看 Streamlit Cloud 應用日誌
2. 檢查 API Keys 是否有效
3. 確認 Secrets 格式正確
4. 參考 `API_KEYS_GUIDE.md` 獲取更多信息

祝您使用愉快！🎉
